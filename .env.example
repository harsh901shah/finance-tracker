# Finance Tracker – example environment variables
# Copy to .env and set values as needed. Do not commit .env.

# Optional: path to SQLite database (default: finance_tracker.db)
# Use an absolute path or a path inside a Docker volume for persistence.
# FINANCE_TRACKER_DB_PATH=/data/finance_tracker.db

# Optional: future use (e.g. API keys, feature flags)
# FINANCE_TRACKER_SECRET_KEY=
# FINANCE_TRACKER_DEBUG=false
